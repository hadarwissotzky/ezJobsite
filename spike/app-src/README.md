Copies of the spike app sources for review. The live app lives at ~/bakeoff-app
(outside the repo) because Expo's EXConstants build script is not space-safe and
every path in this repo contains spaces. spike/app is a symlink to it.
